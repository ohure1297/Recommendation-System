# Recommendation-System
Recommendation API for IT-JOBS-FINDER

How To Run:
1. Run job_embedding_update.py on VSCode
2. Open Command Line in VSCode and run:
python -m uvicorn main:app --reload --port 8000